# Server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Jira configuration (REQUIRED for ticket endpoints)
# Base URL of your Jira Cloud or Server instance, without a trailing slash, e.g.:
#   https://your-domain.atlassian.net
JIRA_BASE_URL=https://your-domain.atlassian.net

# Jira user email that the API token belongs to
JIRA_EMAIL=your-email@example.com

# Jira API token (create from https://id.atlassian.com/manage-profile/security/api-tokens)
JIRA_API_TOKEN=your-api-token

# Cache settings (optional)
# TTL in seconds for in-memory cache of Jira responses (default 300)
CACHE_TTL_SECONDS=300
